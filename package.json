{
  "name": "hgv-reference-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static HGV reference website compatibility wrapper for preview environments.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "start": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
